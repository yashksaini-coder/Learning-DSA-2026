#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  vector<int> data{1, 2, 3, 4};

  for (auto n : data) {
    cout << n << endl;
  }

  return 0;
}
