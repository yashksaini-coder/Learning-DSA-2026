#include <bits/stdc++.h>
#include <iostream>
#include <vector>

using namespace std;

void generate_trianlge() {
  vector<int> data{1};

  for (auto number : data) {
    cout << number << ' ';
  }
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  generate_trianlge();

  return 0;
}
